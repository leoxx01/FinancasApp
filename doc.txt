# recurso de conversão de .ui para .py

python -m PyQt5.uic.pyuic  -x <nome>.ui -o <nome>.py

# gerador de arquivo das libs

pip freeze > requirements.txt

# instalador de libs dentro do arquivo gerado

pip install -r requirements.txt